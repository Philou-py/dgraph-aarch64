image: registry.gitlab.com/ratio-case/docker/docker-buildx:latest

variables:
  DOCKER_HOST: "tcp://docker:2375"
  DOCKER_DRIVER: "overlay2"
  REPO: "${CI_REGISTRY}"
  DEPLOY: "1"
  TARGET: "dgraph"
  TAG: ${CI_JOB_ID}
  ARGS: "--no-cache"
  PLATFORM: "linux/arm64"

services:
  - docker:dind

stages:
  - build

before_script:
  - echo ${CI_JOB_TOKEN} | docker login -u gitlab-ci-token --password-stdin ${CI_REGISTRY}
  - docker buildx create --use --name mybuilder
  - chmod +x *.sh

build:docker:
  stage: build
  script:
    - . ./build.sh
